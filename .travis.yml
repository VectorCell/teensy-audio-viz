sudo: false
language: c++
compiler:
    - g++
addons:
    apt:
        sources:
            - team-gcc-arm-embedded
        packages:
            - gcc-arm-embedded
            - libusb-dev
script:
    - make clean all
